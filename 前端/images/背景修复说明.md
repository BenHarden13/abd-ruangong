# 背景修复说明

## ✅ 问题已解决

### 问题原因
`modern-design.css` 中的全局 `body` 样式设置了首页的背景图片，这会覆盖其他页面（dashboard、profile）的背景设置。

### 解决方案
为每个页面的 `<body>` 标签添加特定的 class，然后使用类选择器应用背景：

---

## 🔧 修改内容

### 1. 首页 (index.html)
```html
<body class="home-page">
```

**CSS (modern-design.css):**
```css
body.home-page {
    background-image: url('../images/hero-background.jpg');
}

body.home-page::before {
    /* 叠加层 */
}
```

---

### 2. 个人档案页 (profile.html)
```html
<body class="profile-page">
```

**CSS (profile.css):**
```css
body.profile-page {
    background-image: url('../images/profile-background.jpg');
}

body.profile-page::before {
    /* 叠加层 */
}
```

---

### 3. 健康仪表盘 (dashboard.html)
```html
<body class="dashboard-page">
```

**CSS (dashboard.css):**
```css
body.dashboard-page {
    background-image: url('../images/dashboard-background.jpg');
}

body.dashboard-page::before {
    /* 深色叠加层 */
}
```

---

## 🎯 现在的效果

### CSS 优先级
1. **基础样式：** `body { ... }` - 所有页面共享（字体、颜色等）
2. **首页样式：** `body.home-page { ... }` - 只在首页生效
3. **档案页样式：** `body.profile-page { ... }` - 只在档案页生效
4. **仪表盘样式：** `body.dashboard-page { ... }` - 只在仪表盘生效

### 不会互相冲突
- ✅ 首页显示健康食材背景
- ✅ 档案页显示花卉图案背景
- ✅ 仪表盘显示科技网络背景

---

## 🚀 测试步骤

### 1. 确保图片已保存
- `images/hero-background.jpg` - 首页
- `images/profile-background.jpg` - 档案页
- `images/dashboard-background.jpg` - 仪表盘

### 2. 清除浏览器缓存
按 `Ctrl + Shift + Delete`，选择"缓存的图像和文件"

### 3. 强制刷新每个页面
按 `Ctrl + F5`

### 4. 访问页面
- http://localhost:8080/index.html
- http://localhost:8080/pages/profile.html
- http://localhost:8080/pages/dashboard.html

---

## 📊 背景对比

| 页面 | Body Class | 背景图片 | 叠加层颜色 | 效果 |
|------|-----------|---------|-----------|------|
| 首页 | `home-page` | 健康食材 | 白色渐变 93% | 清新自然 |
| 档案页 | `profile-page` | 花卉图案 | 白色渐变 94% | 温馨柔和 |
| 仪表盘 | `dashboard-page` | 科技网络 | 深蓝灰 88% | 专业科技 |

---

## ✨ 优势

### 使用 Class 选择器的好处
1. **特异性更高：** `body.home-page` 优先级高于 `body`
2. **不会冲突：** 每个页面独立的背景设置
3. **易于维护：** 清晰的页面标识
4. **灵活扩展：** 未来可以轻松添加其他页面

### 代码结构
```
modern-design.css
  ├─ body { /* 基础样式 */ }
  └─ body.home-page { /* 首页背景 */ }

profile.css
  └─ body.profile-page { /* 档案页背景 */ }

dashboard.css
  └─ body.dashboard-page { /* 仪表盘背景 */ }
```

---

*修复时间：2025年10月13日*
*现在所有页面都有独立的背景，不会互相干扰！*
